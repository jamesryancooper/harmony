name: Quality Weight Gates

on:
  pull_request:
    paths:
      - '.harmony/quality/CHARTER.md'
      - '.harmony/quality/weights/**'
      - '.harmony/quality/policy/**'
      - '.harmony/quality/scores/**'
      - '.harmony/quality/_ops/scripts/compute-quality-score.sh'
      - '.harmony/quality/_ops/scripts/quality-gate.sh'
      - '.harmony/runtime/crates/quality_tools/**'
      - '.harmony/runtime/crates/Cargo.toml'
      - '.github/workflows/quality-weight-gates.yml'
  push:
    branches:
      - main
    paths:
      - '.harmony/quality/CHARTER.md'
      - '.harmony/quality/weights/**'
      - '.harmony/quality/policy/**'
      - '.harmony/quality/scores/**'
      - '.harmony/quality/_ops/scripts/compute-quality-score.sh'
      - '.harmony/quality/_ops/scripts/quality-gate.sh'
      - '.harmony/runtime/crates/quality_tools/**'
      - '.harmony/runtime/crates/Cargo.toml'
      - '.github/workflows/quality-weight-gates.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  score-and-gate:
    name: score-and-gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Resolve baseline files
        shell: bash
        run: |
          set -euo pipefail

          BASE_SHA=""
          if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
          elif [[ "${GITHUB_EVENT_NAME}" == "push" ]]; then
            BASE_SHA="${{ github.event.before }}"
          fi

          if [[ -z "$BASE_SHA" || "$BASE_SHA" == "0000000000000000000000000000000000000000" ]]; then
            echo "No usable baseline SHA; running without baseline comparisons."
            exit 0
          fi

          if git cat-file -e "$BASE_SHA:.harmony/quality/weights/weights.yml" 2>/dev/null; then
            BASE_WEIGHTS="$RUNNER_TEMP/base-weights.yml"
            git show "$BASE_SHA:.harmony/quality/weights/weights.yml" > "$BASE_WEIGHTS"
            echo "BASE_WEIGHTS=$BASE_WEIGHTS" >> "$GITHUB_ENV"
          fi

          if git cat-file -e "$BASE_SHA:.harmony/quality/scores/scores.yml" 2>/dev/null; then
            BASE_SCORES="$RUNNER_TEMP/base-scores.yml"
            git show "$BASE_SHA:.harmony/quality/scores/scores.yml" > "$BASE_SCORES"
            echo "BASE_SCORES=$BASE_SCORES" >> "$GITHUB_ENV"
          fi

          if git cat-file -e "$BASE_SHA:.harmony/quality/CHARTER.md" 2>/dev/null; then
            BASE_CHARTER="$RUNNER_TEMP/base-charter.md"
            git show "$BASE_SHA:.harmony/quality/CHARTER.md" > "$BASE_CHARTER"
            echo "BASE_CHARTER=$BASE_CHARTER" >> "$GITHUB_ENV"
          fi

      - name: Compute weighted scorecard
        run: |
          OUT_DIR="$RUNNER_TEMP/quality-weight-scorecard"
          cargo build --quiet --manifest-path .harmony/runtime/crates/Cargo.toml -p harmony_quality_tools
          .harmony/quality/_ops/scripts/compute-quality-score.sh \
            --weights .harmony/quality/weights/weights.yml \
            --scores .harmony/quality/scores/scores.yml \
            --charter .harmony/quality/CHARTER.md \
            --context .harmony/quality/weights/inputs/context.yml \
            --profile ci-reliability \
            --run-mode ci \
            --maturity beta \
            --repo harmony \
            --out-dir "$OUT_DIR"
          echo "OUT_DIR=$OUT_DIR" >> "$GITHUB_ENV"

      - name: Run weighted quality gate
        shell: bash
        run: |
          set -euo pipefail
          args=(
            --scorecard "$OUT_DIR/scorecard.yml"
            --weights .harmony/quality/weights/weights.yml
            --scores .harmony/quality/scores/scores.yml
            --charter .harmony/quality/CHARTER.md
            --mode ci
            --summary-out "$OUT_DIR/gate-summary.md"
          )

          if [[ -n "${BASE_WEIGHTS:-}" ]]; then
            args+=(--baseline-weights "$BASE_WEIGHTS")
          fi
          if [[ -n "${BASE_SCORES:-}" ]]; then
            args+=(--baseline-scores "$BASE_SCORES")
          fi
          if [[ -n "${BASE_CHARTER:-}" ]]; then
            args+=(--baseline-charter "$BASE_CHARTER")
          fi
          if [[ -n "${BASE_WEIGHTS:-}" && -n "${BASE_SCORES:-}" && -n "${BASE_CHARTER:-}" ]]; then
            args+=(--strict-warnings)
          fi

          .harmony/quality/_ops/scripts/quality-gate.sh "${args[@]}"

      - name: Upload scorecard artifacts
        uses: actions/upload-artifact@v4
        with:
          name: quality-weight-scorecard
          path: |
            ${{ env.OUT_DIR }}/scorecard.yml
            ${{ env.OUT_DIR }}/scorecard.md
            ${{ env.OUT_DIR }}/effective-weights.yml
            ${{ env.OUT_DIR }}/regressions.md
            ${{ env.OUT_DIR }}/gate-summary.md
          retention-days: 30
