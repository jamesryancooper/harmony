# Runtime Contracts

Machine-readable contract surfaces that harden assurance runtime behavior.

## Files

- `alignment-profiles.yml`: canonical alignment profile registry for local and
  CI entrypoints.
- `github-action-pin-policy.yml`: immutable-ref policy for high-trust GitHub
  workflow actions.
