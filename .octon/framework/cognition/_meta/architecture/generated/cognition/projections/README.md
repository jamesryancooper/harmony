# Generated Cognition Projections

Projection definitions and materialized read models are derived cognition
outputs.

- `definitions/**` is committed by default
- `materialized/**` rebuilds locally by default

Materialized outputs must remain reproducible from the committed definitions
and canonical source surfaces.
