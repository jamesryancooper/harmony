# Cognition Runtime Index
# Machine-readable discovery index for cognition runtime surfaces.

schema_version: "1.0"
files:
  - id: runtime-overview
    path: README.md
    summary: Runtime surface orientation and ownership boundaries.
    when: First entrypoint for runtime artifact discovery.

  - id: runtime-reference-primitives
    path: context/reference/primitives.md
    summary: Runtime primitives and route vocabulary reference.
    when: Before interpreting runtime control and evidence terms.

  - id: runtime-reference-tools
    path: context/reference/tools.md
    summary: Runtime tool and surface reference.
    when: When reconciling runtime surface names and capability language.

  - id: runtime-reference-compaction
    path: context/reference/compaction.md
    summary: Runtime compaction and context-shaping guidance.
    when: When reasoning about context preparation or runtime compaction.
