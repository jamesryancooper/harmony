# Bundle Contract

This bundle produces explanation output only. It may inform an operator, but it
does not change proposal lifecycle state, create receipts, mutate child
registries, authorize implementation, or create runtime control truth.
