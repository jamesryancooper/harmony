# Decisions

- Use the pack-local prompt assets as the runtime source of prompt truth.
- Preserve the 4-stage prompt pipeline.
- Keep the alignment audit and executable-prompt generator as companions, not
  numbered stages.
- Prefer the composite skill as the reusable core and the command wrapper as
  the v1 invocation gateway.
