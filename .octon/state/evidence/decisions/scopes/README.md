# Scope Decision Evidence

`state/evidence/decisions/scopes/<scope-id>/**` is the canonical home for
scope-bound operational decision evidence.

Per-scope decision bundles are created only when real scope-level
allow/block/escalate decisions are emitted.
