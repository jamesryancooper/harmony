# Decision Record Plan

## Proposal-stage posture

No new decision artifact is required just to review this proposal packet.

## If the packet is accepted

One repo-owned decision artifact is recommended to record:

- the introduction of a canonical Git/worktree autonomy contract
- the decision to make `ready_pr` explicitly handled rather than implied
- the decision to make remediation new-commit-only
- the decision to move `git-pr-ship.sh` to status-first, explicit-action
  semantics

## Suggested durable location

- `/.octon/instance/cognition/decisions/`

## Suggested decision scope

Keep the decision narrow. It should capture workflow-contract hardening, not a
general history of GitHub automation in the repo.
