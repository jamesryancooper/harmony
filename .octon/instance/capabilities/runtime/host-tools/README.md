# Repo Host-Tool Requirements

This surface contains repo-owned desired requirements for host-scoped external
tools.

- desired requirements live here under `instance/**`
- actual installs and provisioning receipts live in the host-scoped Octon home
  outside the repo
- shared framework tool contracts live under
  `framework/capabilities/runtime/host-tools/**`

The primary requirements manifest is `requirements.yml`.
