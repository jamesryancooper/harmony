schema_version: "1.0"

services:
  - id: guard
    display_name: Guard
    path: governance/guard/
    summary: "Harness-native content safety checks for injection, hallucination, secrets, and PII."
    status: active
    interface_type: shell
    tags: [safety, regex, detection]
    category: guard
    stateful: false

  - id: prompt
    display_name: Prompt
    path: modeling/prompt/
    summary: "Harness-native prompt compilation and hashing service."
    status: active
    interface_type: shell
    tags: [prompt, template, tokens]
    category: prompt
    stateful: false

  - id: cost
    display_name: Cost
    path: operations/cost/
    summary: "Harness-native budget estimation and usage recording service."
    status: active
    interface_type: shell
    tags: [budget, cost, tracking]
    category: cost
    stateful: true

  - id: flow
    display_name: Flow
    path: planning/flow/
    summary: "Harness-native flow execution orchestrator for HTTP runner endpoints."
    status: active
    interface_type: mcp
    tags: [workflow, orchestration, langgraph]
    category: flow
    stateful: false
