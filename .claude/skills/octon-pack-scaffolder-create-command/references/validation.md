# Validation

Success means:

- the command markdown exists at the expected path
- the manifest fragment entry uses `access: agent`
- lexical ordering is preserved
- rerunning is idempotent
- conflicting content blocks the scaffold
